import summation

def test_summation():

   # It’s basically saying: “Check that the result of summation.summation(4) is equal to 5. If it isn’t, raise an error.” 
    assert summation.summation(4) == 5

